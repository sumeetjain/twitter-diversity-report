<div class="form">
  <h2>CURRENT INFORMATION FOR: <span class="bluetext" style="font-family:georgia"><%= session[:screen_name] -%></span></h2> 

  <ul>
    <li><strong>EDUCATION LEVEL:</strong><span style="font-family:georgia"> <%= @user.educations[0] ? @user.educations[0].value : "Unanswered"  %></span></li>
    <li><strong>INCOME LEVEL:</strong><span style="font-family:georgia"> <%= @user.incomes[0] ? @user.incomes[0].value : "Unanswered" %></span></li>    
    <li><strong>BIRTH YEAR:</strong><span style="font-family:georgia"> <%= @user.ages[0] ? @user.ages[0].value : "Unanswered" %></span></li>
    <li><strong>GENDER/S:</strong><span style="font-family:georgia"> <% b = [] %> 
                                   <% @user.genders.each do |object| b.push object.value end %>
                                   <%= b.join(", ") %></span></li>
    <li><strong>ETHNICITY/IES:</strong><span style="font-family:georgia"> <% b = [] %> 
                                   <% @user.ethnicities.each do |object| b.push object.value end %>
                                   <%= b.join(", ") %></span></li>
    <li><strong>ORIENTATION/S:</strong><span style="font-family:georgia"> <% b = [] %> 
                                   <% @user.orientations.each do |object| b.push object.value end %>
                                   <%= b.join(", ") %></span></li>
    
  </ul> 
  

  <h2><a href="<%= "#{@user.id}" %>/edit"><img class="arrow" src="/assets/arrow1.png">EDIT/UPDATE YOUR INFORMATION</a></h2>
  
  <h2><img class="arrow" src="/assets/arrow1.png"><%= link_to 'DELETE YOUR INFORMATION', url_for(action: :delete,id: @user.id),method: :delete, data: {confirm: "Are you sure you want to remove your information? (Specifically-identifying information is encrypted.)"} %></h2>

</div>

